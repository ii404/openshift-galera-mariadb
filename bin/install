#!/bin/bash -e

source $OPENSHIFT_CARTRIDGE_SDK_BASH
source "$OPENSHIFT_MARIADB_DIR/lib/mysql_context"

case "$1" in
  -v|--version)
    version="$2"
esac

echo "install=$OPENSHIFT_GEAR_DNS - has finished install" 
